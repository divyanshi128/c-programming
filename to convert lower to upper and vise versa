#include <stdio.h>

int main()
{
    char a[20];
    scanf(“%[^\n]%*c,a”);
    for(int i=0;a[i];i++){
    if(65<=a[i]&& a[i]<=90)
    a[i]=a[i]+32;
    else if(97<=a[i] && a[i]<=122)
    a[i]=a[i]-32;
    }
    printf("%s",a);
    return 0;
}
